---
import Layout from '../layouts/Layout.astro';
import WebSocketChat from '../components/react/WebSocketChat.jsx';
---

<Layout title="WebSocket Demo">
  <div class="container">
    <h1>WebSocket Demo with Deno</h1>
    <p class="description">
      This is a simple demonstration of using Deno's WebSocket API with Astro server endpoints.
      Type a message in the box below and see the server echo it back to you.
    </p>
    
    <div class="websocket-container">
      <WebSocketChat client:load />
    </div>
    
    <div class="explanation">
      <h2>How it works</h2>
      <p>
        This demo uses a WebSocket connection established through an Astro server endpoint. The backend is powered by Deno's native WebSocket API.
      </p>
      <p>
        When you send a message, it's transmitted to the server via WebSocket, which then echoes it back with a timestamp.
      </p>
    </div>
    
    <div class="back-link">
      <a href="/">Back to Home</a>
    </div>
  </div>
</Layout>

<style>
  .container {
    max-width: 800px;
    margin: 0 auto;
    padding: 2rem 1rem;
  }
  
  h1 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: #333;
    text-align: center;
  }
  
  .description {
    text-align: center;
    color: #666;
    margin-bottom: 2rem;
    line-height: 1.5;
  }
  
  .websocket-container {
    margin: 2rem 0;
  }
  
  .explanation {
    margin: 2rem 0;
    padding: 1.5rem;
    background-color: #f8f9fa;
    border-radius: 8px;
    border-left: 4px solid #4169e1;
  }
  
  .explanation h2 {
    margin-bottom: 1rem;
    color: #333;
  }
  
  .explanation p {
    color: #666;
    margin-bottom: 1rem;
    line-height: 1.6;
  }
  
  .back-link {
    margin-top: 2rem;
    text-align: center;
  }
  
  .back-link a {
    display: inline-block;
    padding: 0.5rem 1rem;
    background-color: #4169e1;
    color: white;
    border-radius: 4px;
    text-decoration: none;
    transition: background-color 0.3s ease;
  }
  
  .back-link a:hover {
    background-color: #3151b5;
  }
</style>